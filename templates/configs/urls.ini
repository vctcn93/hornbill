# 犀鸟 (Hornbill) - 插件配置文件
# 用户只改这个文件。install.bat 编译前会自动复制到 src/core/。

# ---- 示例：API 端点 ----
[api]
base_url = https://api.example.com/v1/
timeout = 30

# ---- 示例：切换数据源（注释掉当前行，取消注释备选行）----
# [datasource]
# provider = default
# url = https://default.example.com/data/{z}/{x}/{y}.png
# key = your-default-key

# provider = alternative
# url = https://alternative.example.com/data/{z}/{x}/{y}.png
# key = your-alternative-key

# ---- 添加你需要的配置段 ----
